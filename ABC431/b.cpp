#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

int main() {
    int X, N;
    cin >> X >> N;
    // ロボットの部品の重さ
    vector<int> W(N);
    for (int i = 0; i < N; i++) cin >> W[i];
    int Q;
    cin >> Q;
    vector<int> P(Q);
    for (int i = 0; i < N; i++) cin >> P[i];

    



}